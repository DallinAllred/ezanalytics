import axios from '@/axiosConfig'

async function checkPermissions(permList) {
    // Get permissions associated with the current session and validate
    // the user is allowed to access the resource
    // Return a boolean
}